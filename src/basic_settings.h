#pragma once

struct BasicSettings{
    float WINDOW_WIDTH = 800;
    float WINDOW_HEIGHT = 600;
    float FPS = 60;
    float ANIMAL_SIZE = 10;
    float PLANT_SIZE = 30;
    float animal_friction = 0.92;
};
