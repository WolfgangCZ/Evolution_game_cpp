#pragma once

const float WINDOW_WIDTH = 800;
const float WINDOW_HEIGHT = 600;
const float FPS = 60;
const float ANIMAL_SIZE = 10;
const float PLANT_SIZE = 30;
const float animal_friction = 0.95;

static bool debug_mode = 0;
